extends: substitution
message: "Use '%s' instead of '%s'."
level: error
ignorecase: true
action:
  name: replace
swap:
  pre-built: prebuilt
  Pre-built: Prebuilt
  builtin: built-in
  Builtin: Built-in
  url: URL
  URLs: URLs
