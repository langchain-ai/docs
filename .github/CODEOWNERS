# GitHub CODEOWNERS — used by GitHub to auto-request reviewers on PRs.
#
# General doc ownership lives in `.github/OWNERS` (used by the
# pr-welcome-comment workflow) and intentionally does NOT use this
# file, so most paths are left unowned here to avoid auto-requesting
# reviewers across the repo.
#
# Only add rules here for areas whose owners explicitly want GitHub
# to auto-request review on every PR.

# Deep Agents CLI docs
/src/oss/deepagents/cli/ @mdrxy @npentrel
