# Specifiers that scripts/check_version_claims.py must not fail on, even though
# the version does not resolve on the registry.
#
# One specifier per line, written exactly as it appears in the page (no
# ecosystem prefix), with a comment giving the reason. Lines starting with #
# are comments. Prefer fixing the page: an entry here is a standing exception,
# so it should describe why the version is unresolvable rather than wrong.

# --- deliberate "any version" floors -------------------------------------
# src/langsmith/setup-app-requirements-txt.mdx pins the gRPC and protobuf stack
# that Agent Server needs, and leaves the OpenTelemetry packages unpinned. 0.0.1
# is a floor meaning "any release", not a version anyone shipped.
opentelemetry-api>=0.0.1
opentelemetry-sdk>=0.0.1
opentelemetry-exporter-otlp-proto-http>=0.0.1

# src/langsmith/deploy-other-frameworks.mdx, same intent for the instrumentation
# packages. Their real version line starts at 0.x with a different scheme.
opentelemetry-instrumentation-openai>=0.1.0
opentelemetry-instrumentation-crewai>=0.1.0

# --- placeholders in "use your own package here" examples ----------------
# src/langsmith/local-dev-testing.mdx shows the shape of a langgraph.json
# dependencies entry. The name is a stand-in, so there is nothing to resolve.
my-package==1.0.0
