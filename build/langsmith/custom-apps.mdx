---
title: Custom Apps
description: Build custom UIs that run inside LangSmith and call the LangSmith API, either by chatting with LangSmith Chat or locally with the LangSmith CLI.
---

<Note>
Custom apps are available exclusively on the **Enterprise** plan.
</Note>

A custom app is a UI you build yourself that runs inside LangSmith and calls the [LangSmith API](/langsmith/smith-api-ref). Deployed apps appear under **Custom Apps** in the sidebar, where everyone with access to the workspace opens them. There is no separate service to host and no separate login.

The LangSmith UI cannot be the right shape for every workflow, and custom apps are the escape hatch. Build a purpose-built annotation surface, a comparison view for the experiments you actually run, or a dashboard scoped to your own traces.

An app runs in a sandbox with no network access of its own. Its requests reach the LangSmith API through the host, carrying the viewer's own credentials, so each person sees only the data their [permissions](/langsmith/organization-workspace-operations) already allow and an app cannot reach services outside LangSmith.

## Build an app

Build an app by [chatting with LangSmith Chat](#build-with-chat) in the browser, or [locally with the CLI](#build-locally-with-the-cli) and your own coding agent. Both paths produce the same app and store the same source, so either one can [edit](#edit-an-app) what the other built.

### Build with chat

Building in the browser has three requirements:

- **Sandboxes**: [sandboxes](/langsmith/sandboxes) must be enabled on your deployment, because each session runs in one.
- **A model API key**: Chat needs a [model API key set for the workspace](/langsmith/chat#get-started).
- **Permissions**: the `custom-apps:update` and `custom-apps:download` permissions, plus the [sandbox permissions](#permissions).

Without all three, the **App** action opens the CLI instructions instead.

To build an app with chat:

<Steps>
<Step title="Start a new app">
Go to **Custom Apps** and select **App**. An empty app is created and its editing workspace opens with chat on the left.
</Step>
<Step title="Describe what you want to build">
Type what the app should do into the composer, and select the model to build with from the menu beside it. The composer accepts a prompt while the sandbox behind it is still starting.
</Step>
<Step title="Review what Chat built">
The **Preview** tab renders the app as the workspace sees it. The **Code** tab holds the file tree, the source, and the changes from the session. A badge counts the files changed since the last deploy.
</Step>
<Step title="Deploy the app">
Select **Deploy**. The first deploy asks for a name, builds the app, and publishes it to the workspace.

An app that has never been deployed is discarded when you leave the page, and LangSmith asks before discarding work.
</Step>
</Steps>

### Build locally with the CLI

Building locally uses the [LangSmith CLI](/langsmith/langsmith-cli), which scaffolds a starter, runs it in a real sandbox on your machine, and uploads it when you are ready. To build an app locally:

<Steps>
<Step title="Install the CLI">
```bash
curl -fsSL https://cli.langsmith.com/install.sh | sh
```

For Windows, Homebrew, and other options, see [Install](/langsmith/langsmith-cli#install).
</Step>
<Step title="Configure your environment">
```bash
export LANGSMITH_ENDPOINT=<your-langsmith-endpoint>
export LANGSMITH_API_KEY=<your-langsmith-api-key>
```
</Step>
<Step title="Scaffold an app">
```bash
langsmith apps init --name my-annotation-view --template annotation-queue
```

`init` creates a directory named after the app and installs its dependencies. It also writes an `AGENTS.md` that gives a coding agent the conventions and API surface it needs to produce a working app on the first pass. Omit `--template` for a blank single-file starter.
</Step>
<Step title="Iterate locally">
```bash
cd my-annotation-view
langsmith apps dev
```

The app runs in the same kind of sandbox it gets inside LangSmith, with API calls proxied through your own credentials. Failed calls and uncaught errors stream to the terminal, so most debugging needs no browser devtools. Add `--verbose` for every successful call and all console output, or `--quiet` to silence the app.
</Step>
<Step title="Push it live">
```bash
langsmith apps push
```

The first push creates the app and writes `.langsmith/app.json` to link the directory to it. Later pushes update the same app. Commit that file so teammates push to the same app.
</Step>
</Steps>

#### Starter templates

Pass one of these to `langsmith apps init --template`:

| Template | What it scaffolds |
|----------|-------------------|
| `annotation-queue` | A queue-review UI: run and thread items, a type-specific viewer, and a feedback rubric. |
| `annotation-queue-grid` | The same review workflow as an editable spreadsheet. |
| `experiment-comparison` | A side-by-side comparison of evaluation experiments against a baseline. |
| `coding-agent-dashboard` | Charts over coding-agent runs: usage, cost, errors, and activity over time. |

## Edit an app

Edit an app in the browser with chat, or pull its source and edit it locally. An app is editable from either side, whichever one created it.

### Edit with chat

Open an app from **Custom Apps** and select **Edit**. The published app is replaced by the editing workspace: chat on the left, and the same **Preview** and **Code** tabs on the right.

Ask for the change in chat, or edit a file directly in **Code** and save it with `Cmd+S` (`Ctrl+S` on Windows and Linux). Either way the preview rebuilds, and the badge in the header counts how many files differ from what is deployed.

Two actions end the session:

- **Deploy**: builds the app and publishes it as a new version for the workspace.
- **Cancel**: leaves the editing workspace. LangSmith asks first when changes have not been deployed.

Editing runs in a sandbox that stops after a period of inactivity. If its sandbox is gone when you return, select **Recreate from saved source** to rebuild the session from the last deployed source. Changes that were never deployed do not survive that.

The **Edit** action in the app list's row menu is a different thing: it renames the app, copies its ID, and changes who it is shared with.

### Edit locally with the CLI

A deploy from the browser stores the app's source, and so does `langsmith apps push`. To edit an app locally, whichever side built it, pull it down:

```bash
langsmith apps pull my-app
npm install --prefix my-app
```

Edit the source with your own coding agent, then run `langsmith apps push` to publish it.

The scaffolded `AGENTS.md` asks the agent to keep a `context.md` at the app root as the app's memory: what the app does, which files matter, and the decisions behind them. Any root file travels with the source, so a teammate who pulls the app starts from what you learned rather than from scratch.

## Share an app with your organization

A custom app belongs to the workspace it was created in. To make one visible from every workspace in the organization, open the row menu in the app list, select **Edit**, then select **Share with organization**. **Pull into this workspace** reverses it.

Shared apps are readable from any workspace in the organization. A workspace app wins over an organization app of the same name. When the workspace already has an app under that name, LangSmith asks for a different one before claiming it.

## CLI reference

| Command | What it does |
|---------|-------------|
| `langsmith apps init --name NAME [--template TEMPLATE]` | Scaffold a starter app in a new directory named after the app, and install its dependencies. |
| `langsmith apps dev` | Run the current directory's app locally in a real sandbox. |
| `langsmith apps push` | Upload the current directory as a custom app, creating it on the first push. Pass `--no-build` to upload the current files as-is. |
| `langsmith apps pull APP_ID_OR_NAME` | Download an app's source into a new directory. |
| `langsmith apps list` | List custom apps. |
| `langsmith apps delete APP_ID_OR_NAME` | Delete an app by ID or name. |

Except for `init`, `pull`, `list`, and `delete`, these commands act on the current directory, so change into the app's directory first.

## Permissions

Custom app access is governed by the `custom-apps:*` permissions. For the full table, see [Custom apps](/langsmith/organization-workspace-operations#custom-apps).

Editing in the browser also requires the `sandboxes:create`, `sandboxes:read`, `sandboxes:update`, `sandboxes:delete`, and `sandboxes:exec` permissions, because each session runs in a sandbox. A user who can read an app but not edit it opens the published app without the **Edit** action.

---

<div className="source-links">
<Callout icon="terminal-2">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Callout>
<Callout icon="edit">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/langsmith/custom-apps.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
</div>
