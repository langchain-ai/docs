---
title: Agent environments
sidebarTitle: Environments
description: How an agent's four environments divide its traces, how to select one, and which parts of the set are fixed for the initial release.
icon: "stack"
---

An _environment_ records which deployment of an application produced a trace. Every [agent](/langsmith/agents) divides its traces across the same four environments, and every trace an agent records lands in exactly one of them.

Because the environment travels with the trace, the deployment that produced it is a property of the trace rather than something to recover from a project name or a saved filter. A question about production behavior never has to exclude test traffic by hand.

## The four environments

An agent's environments come from a fixed set of four. Listed most production-like first, they are Production, Staging, Development, and Local, which is the order the environment switcher uses.

LangSmith attaches no behavior to the choice beyond keeping each environment's traces apart. An environment does not change how an agent is instrumented, and the same build can report into a different environment through tracing configuration alone.

<Note>
The four environments are the set for the initial release rather than the data model. Custom environment names are out of scope for launch.
</Note>

## Select an environment

Wherever LangSmith needs one environment rather than all of them, it uses the same control: a dropdown whose placeholder reads **Select environment** and whose accessible name is Environment. It lists the four labels in the order above, most production-like first.

The list holds the environments that exist for the agent. If an agent has none, the control does not render, and there is no empty state to read.

{/*
  DOC-1597. Do not restore the stronger claim that the list holds only the environments
  the agent has recorded traces for, and do not weaken this to "all four" either. Lazy
  versus eager creation is an open product decision as of 2026-09-15: the Interrupt launch
  checklist carries "Decide if we want to auto-create 4 environments for each agent" and a
  dependent P1 on quickstarts for environments that do not exist yet (LSO-4211).

  Evidence for lazy, from the dev build of 2026-09-16: an agent created in the Build tab
  shows exactly one row in the agent Overview's Tracing section, Production, immediately
  after its setup conversation is traced. Not four. One agent is not proof, but it is the
  only observation there is, and it points away from eager creation.

  The sentence above is true either way. If the decision lands before the 09-23 merge,
  make it specific.
*/}

This is the control behind environment selection on tracing, dashboards, alerts, [Engine](/langsmith/engine-overview), Studio, and deployments. [Evaluators](/langsmith/evaluators) and [Insights](/langsmith/insights) also scope to a single environment, but not through this dropdown.

{/*
  DOC-1597. The surface list is the component tree at langchainplus c20ab6df46a, not a
  capture. EnvironmentSwitcher is rendered under Pages/Project (tracing), Pages/Dashboards
  and Pages/SingleDashboard, components/Alerts, Pages/ForgeIssueBoards (Engine), Pages/Graph
  (Studio), and Pages/HostProject and Pages/HostProjects (deployments).

  It had previously read "dashboards, alerts, evaluators, and Insights", which was wrong in
  both directions. Pages/EvaluatorDetails and Pages/InsightsDashboard contain no environment
  references at all, and the four surfaces that do use the control were missing. Those two
  pages still scope to one environment, which is why they keep a sentence here, but they do
  it without this dropdown, so do not link them to this section as though they had one.
*/}

## See also

- [Agents](/langsmith/agents)
- [Observability concepts](/langsmith/observability-concepts)
- [Log traces to a project](/langsmith/log-traces-to-project)

---

<div className="source-links">
<Callout icon="terminal-2">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Callout>
<Callout icon="edit">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/langsmith/agent-environments.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
</div>
