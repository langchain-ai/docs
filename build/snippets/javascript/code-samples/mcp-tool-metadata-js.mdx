```typescript
import type { DynamicStructuredTool } from "@langchain/core/tools";

function isDestructive(tool: DynamicStructuredTool): boolean {
  return tool.metadata?.mcp?.tool?.annotations?.destructiveHint ?? false;
}
```
