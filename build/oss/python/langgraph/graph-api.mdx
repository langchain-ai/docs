---
title: Graph API overview
sidebarTitle: Graph API
description: Learn how LangGraph models agent workflows as graphs of shared state, nodes, and edges.
---

import GraphApiUsingTasksOriginalJs from '/snippets/python/code-samples/graph-api-using-tasks-original-js.mdx';
import GraphApiUsingTasksOriginalPy from '/snippets/python/code-samples/graph-api-using-tasks-original-py.mdx';
import GraphApiUsingTasksTaskJs from '/snippets/python/code-samples/graph-api-using-tasks-task-js.mdx';
import GraphApiUsingTasksTaskPy from '/snippets/python/code-samples/graph-api-using-tasks-task-py.mdx';

The Graph API defines agent workflows as a graph of [state](#state), [nodes](#nodes), and [edges](#edges). [`Send`](#send) and [`Command`](#command) control how the graph runs work: `Send` dynamically invokes nodes for fan-out, and `Command` updates state while routing to the next node (including resume after an interrupt). Use the Graph API when you want explicit shared state and a visual control flow.

For step-by-step recipes, see [Use the graph API](/oss/python/langgraph/use-graph-api). To choose between this and the Functional API, see [Choosing between Graph and Functional APIs](/oss/python/langgraph/choosing-apis).

## Graphs

LangGraph models agent workflows as graphs built from three parts:

1. [`State`](#state): A shared snapshot of the application. Usually a typed schema.
2. [`Nodes`](#nodes): Functions that read state, do work, and return updates.
3. [`Edges`](#edges): Fixed or conditional transitions that determine the next node.

Compose nodes and edges to build sequences, branches, and loops. Nodes and edges are ordinary functions. They can call an LLM or run other code.

In short: nodes do the work; edges decide what runs next.

### Super-steps

LangGraph runs graphs with [message passing](https://en.wikipedia.org/wiki/Message_passing), in the style of Google's [Pregel](https://research.google/pubs/pregel-a-system-for-large-scale-graph-processing/) system. Execution advances in discrete **super-steps**:

- Nodes that run in parallel share one super-step.
- Nodes that run in sequence use separate super-steps.
- A node becomes active when it receives input on an incoming edge.
- After a super-step, nodes with no pending messages become inactive.
- The run ends when every node is inactive and no messages remain in transit.

### StateGraph

[`StateGraph`](https://reference.langchain.com/python/langgraph/graph/state/StateGraph) is the main graph builder. Parameterize it with your state schema.

### Compiling your graph

Build a graph by defining [state](#state), adding [nodes](#nodes) and [edges](#edges), then compiling. Compilation checks the graph structure (for example for orphaned nodes). It is also where you attach runtime options such as [checkpointers](/oss/python/langgraph/persistence) and breakpoints.

You must compile the graph before you invoke or stream it:

```python
graph = graph_builder.compile(...)
```




## State

State is the shared data that nodes read and update. LangGraph stores that data in **channels**: named slots in the state schema (the keys of your state object). Each channel holds a value and defines how updates merge into it. Most channels use last-write-wins. Channels with a [reducer](#reducers) combine values instead, for example by appending to a list.

State has two parts:

- A [schema](#schema) that declares the channels in the graph
- Optional [reducers](#reducers) that control how updates merge into each channel

Nodes return partial updates. LangGraph applies those updates through each channel's reducer.

Define state with a [`TypedDict`](https://docs.python.org/3/library/typing.html#typing.TypedDict), a [`dataclass`](https://docs.python.org/3/library/dataclasses.html) (for defaults), or a Pydantic [`BaseModel`](/oss/python/langgraph/graph-api-state#use-pydantic-models-for-graph-state) when you need validation. Prefer `TypedDict` or `dataclass` for performance.

<Info>
[`create_agent`](/oss/python/langchain/agents) state schemas must be `TypedDict`. Pydantic models are not supported.
</Info>




### Schema

By default, the graph input and output schemas match the full state. You can narrow them when some keys are internal only. See [Define input and output schemas](/oss/python/langgraph/graph-api-state#define-input-and-output-schemas).

#### Multiple schemas

Most graphs share one state schema. Use multiple schemas when you need either of the following:

- Private channels that internal nodes share, but that are not part of graph input or output
- Explicit input and output schemas that are subsets of the full internal state

A node can write to any channel in the graph state, including channels outside its declared input schema. Nodes can also introduce private channels when those schemas are defined. Input and output schemas constrain what `invoke` accepts and returns. They do not hide channels from [streaming](/oss/python/langgraph/streaming) by default. See [Private channels and streaming](/oss/python/langgraph/graph-api-state#private-channels-and-streaming).

For examples, see [Define input and output schemas](/oss/python/langgraph/graph-api-state#define-input-and-output-schemas) and [Pass private state between nodes](/oss/python/langgraph/graph-api-state#pass-private-state-between-nodes).

### Reducers

Reducers control how node updates merge into state. Each key has its own reducer. With no reducer, updates overwrite the previous value.

#### Reducer arguments

A reducer is a binary function:

- **Left**: The current value in state for that key
- **Right**: The update returned by a node for that key

```python
new_value = reducer(left=current_state[key], right=node_update[key])
```




#### Default reducer

The default reducer replaces the left value with the right. Nodes return partial updates, not the full state.

For example, start with `{"foo": 1, "bar": ["hi"]}`. An update of `{"foo": 2}` yields `{"foo": 2, "bar": ["hi"]}`. A later `{"bar": ["bye"]}` yields `{"foo": 2, "bar": ["bye"]}`.

#### Custom reducers

Use a custom reducer to combine values, such as appending to a list.

```python
import operator
from typing import Annotated

from typing_extensions import TypedDict


class State(TypedDict):
    foo: int
    bar: Annotated[list[str], operator.add]
```

Here `foo` overwrites. `bar` appends with `operator.add`. From `{"foo": 1, "bar": ["hi"]}`, an update of `{"bar": ["bye"]}` becomes `{"foo": 1, "bar": ["hi", "bye"]}`.




#### Overwrite

To bypass a merging reducer and replace a value (including clearing it), use [`Overwrite`](https://reference.langchain.com/python/langgraph/types/). See [Bypass reducers with Overwrite](/oss/python/langgraph/graph-api-state#bypass-reducers-with-overwrite).




#### Resetting a reducer field

With a merging reducer, returning an empty value keeps prior data. That matters for error buffers or retry counters that must clear between attempts. See [Process state updates with reducers](/oss/python/langgraph/graph-api-state#process-state-updates-with-reducers) and [Bypass reducers with Overwrite](/oss/python/langgraph/graph-api-state#bypass-reducers-with-overwrite).



### Working with messages in graph state

#### Why use messages?

Chat models take a list of messages as input. LangChain's [chat model interface](/oss/python/langchain/models) accepts message objects such as [`HumanMessage`](https://reference.langchain.com/python/langchain-core/messages/human/HumanMessage) and [`AIMessage`](https://reference.langchain.com/python/langchain-core/messages/ai/AIMessage). See the [Messages conceptual guide](/oss/python/langchain/messages).

#### Using messages in your graph

Store conversation history as a list of messages in state, with a reducer that appends new messages and updates existing ones by ID.

Use [`add_messages`](https://reference.langchain.com/python/langgraph/graph/message/add_messages) on a `messages` key. Plain `operator.add` only appends, so it cannot update an existing message in place during human-in-the-loop edits.

```python
from typing import Annotated

from langchain.messages import AnyMessage
from langgraph.graph.message import add_messages
from typing_extensions import TypedDict


class GraphState(TypedDict):
    messages: Annotated[list[AnyMessage], add_messages]
```




#### Serialization

Message reducers also deserialize common wire formats into LangChain message objects.

```python
# Supported
{"messages": [HumanMessage(content="message")]}

# Also supported
{"messages": [{"type": "human", "content": "message"}]}
```

Access attributes with dot notation, for example `state["messages"][-1].content`. See [LangChain serialization](/oss/python/langchain/messages#serialization).




#### MessagesState

`MessagesState` is a prebuilt state with a single `messages` key that uses [`add_messages`](https://reference.langchain.com/python/langgraph/graph/message/add_messages). Subclass it to add fields:

```python
from langgraph.graph import MessagesState


class State(MessagesState):
    documents: list[str]
```


## Nodes

Nodes are sync or async functions that implement a step of the graph.

A node can accept:

1. `state`: The graph [state](#state)
2. `config`: A [`RunnableConfig`](https://reference.langchain.com/python/langchain-core/runnables/config/RunnableConfig) with values such as `thread_id` and `tags`
3. `runtime`: A `Runtime` object with [runtime context](#runtime-context), `store`, `stream_writer`, `execution_info`, and related helpers

Add nodes with [`add_node`](https://reference.langchain.com/python/langgraph/graph/state/StateGraph/add_node):

```python
from dataclasses import dataclass

from langgraph.graph import StateGraph
from langgraph.runtime import Runtime
from typing_extensions import TypedDict


class State(TypedDict):
    input: str
    results: str


@dataclass
class Context:
    user_id: str


builder = StateGraph(State)


def plain_node(state: State):
    return state


def node_with_runtime(state: State, runtime: Runtime[Context]):
    return {"results": f"Hello, {state['input']}!"}


builder.add_node("plain_node", plain_node)
builder.add_node("node_with_runtime", node_with_runtime)
```




Behind the scenes, LangGraph wraps functions as [`RunnableLambda`](https://reference.langchain.com/python/langchain-core/runnables/base/RunnableLambda) so they gain batching, async support, and [tracing](/langsmith/observability).

If you omit a node name, LangGraph uses the function name.

```python
builder.add_node(my_node)
# Reference the node as "my_node" in edges
```




### Re-execution and idempotency

With a [checkpointer](/oss/python/langgraph/persistence), LangGraph saves checkpoints at [super-step](#graphs) boundaries, not mid-function inside a node. After an [interrupt](/oss/python/langgraph/interrupts) or [retry](/oss/python/langgraph/fault-tolerance#retries), the affected node runs again from the start of its function. Side effects before the pause run again.

Design node logic so re-execution is safe. Prefer idempotency keys, upserts, or read-before-write checks. For effects around `interrupt()`, see [Side effects called before `interrupt` must be idempotent](/oss/python/langgraph/interrupts#side-effects-called-before-interrupt-must-be-idempotent).

You can add or remove nodes and edges without breaking resume for existing threads. Resumed runs use saved state and the graph you compile now.

If a node calls [tasks](/oss/python/langgraph/functional-api#task) or [`interrupt`](https://reference.langchain.com/python/langgraph/types/interrupt), stricter [determinism](/oss/python/langgraph/functional-api#determinism) rules apply on resume. Completed task results are restored from the checkpointer. Changing task or interrupt order before the resume point can mismatch cached values. See [Idempotency](/oss/python/langgraph/functional-api#idempotency) and [Using tasks in nodes](#using-tasks-in-nodes).

### Using tasks in nodes

If one node contains several operations, implement each operation as a [task](/oss/python/langgraph/functional-api#task) instead of splitting across nodes. With a checkpointer, completed task results are saved, so a resumed thread can skip finished work inside the node.

<Tabs>
    <Tab title="Original">
    <GraphApiUsingTasksOriginalPy />
    </Tab>
    <Tab title="With task">
    <GraphApiUsingTasksTaskPy />
    </Tab>
</Tabs>




### `START` node

[`START`](https://reference.langchain.com/python/langgraph/constants/START) is the virtual entry that delivers user input into the graph. Use it to mark which nodes run first.

```python
from langgraph.graph import START

graph.add_edge(START, "node_a")
```




### `END` node

`END` marks a terminal edge. Use it when a path should stop.

```python
from langgraph.graph import END

graph.add_edge("node_a", END)
```




### Node caching

LangGraph can cache node (and task) results by input. Pass a cache when you compile the graph, and attach a [`CachePolicy`](https://reference.langchain.com/python/langgraph/types/CachePolicy) on the node.

For options, end-to-end examples, and graph-wide defaults, see [Add node caching](/oss/python/langgraph/graph-api-nodes#add-node-caching).

## Edges

Edges decide where control goes next:

- **Normal edges**: Always go from one node to the next
- **Conditional edges**: Call a function to determine the next node(s)
- **Entry point**: First node after user input (`START` → node)
- **Conditional entry point**: Route from `START` with a function

A node can have multiple outgoing edges. Destinations on those edges run in parallel in the next super-step.

<Warning>
For each node, use either static edges or dynamic routing ([conditional edges](#conditional-edges) / [`Command`](https://reference.langchain.com/python/langgraph/types/Command)), not both. Mixing them can run both paths.
</Warning>

### Normal edges

```python
graph.add_edge("node_a", "node_b")
```




### Conditional edges

```python
graph.add_conditional_edges("node_a", routing_function)
# Or with an explicit map:
graph.add_conditional_edges(
    "node_a",
    routing_function,
    {"path_a": "node_b", "path_b": "node_c"},
)
```




The routing function can return a node name, `END`, a list of names (fan-out), or [`Send`](https://reference.langchain.com/python/langgraph/types/Send) objects for map-reduce. See [Build sequences, branches, and loops](/oss/python/langgraph/graph-api-control-flow).

### Entry point

```python
graph.add_edge(START, "node_a")
```




### Conditional entry point

```python
graph.add_conditional_edges(START, routing_function)
```




## `Send`

Use [`Send`](https://reference.langchain.com/python/langgraph/types/Send) when you need to run the same node once per item, each with its own input, and the number of items is only known at runtime. That shape is often called map-reduce or orchestrator-worker: one node produces a list, workers process each item in parallel, then a later node reads the combined results.

Normal edges cannot do this. They choose which nodes run next, but every chosen node still receives the shared graph state, and the topology is fixed when you build the graph.

Return one or more `Send` objects from a conditional edge. Each `Send` names a target node and passes a custom argument as that invocation's input. LangGraph runs those invocations in parallel. When workers write to shared state keys, [reducers](#reducers) merge their updates, for example by concatenating lists.

The first argument to `Send` is the target node name. The second is the state (or partial state) for that invocation.

```python
from langgraph.types import Send


def continue_to_jokes(state: OverallState):
    return [Send("generate_joke", {"subject": s}) for s in state["subjects"]]


graph.add_conditional_edges("node_a", continue_to_jokes)
```




For a full map-reduce example, see [Map-Reduce and the Send API](/oss/python/langgraph/graph-api-send-command#map-reduce-and-the-send-api).

## `Command`

Use [`Command`](https://reference.langchain.com/python/langgraph/types/Command) when a single step must both update state and choose what runs next. Normally those jobs are separate: a node returns state updates, and [edges](#edges) (or [conditional edges](#conditional-edges)) decide the next node. Return `Command` from a node when the next hop depends on work you just did in that same function.

`Command` also covers two other cases that plain returns and edges do not: resume a run after an [interrupt](/oss/python/langgraph/interrupts), and update or route from inside a [tool](#return-from-tools).

For routing alone, prefer conditional edges. Reach for `Command` when you need the update and the route together (or one of the interrupt / tool cases above).

`Command` accepts:

- **`update`**: State updates (like a normal node return)
- **`goto`**: Next node(s) (like a conditional edge)
- **`graph`**: Parent graph target when navigating from a [subgraph](/oss/python/langgraph/use-subgraphs)
- **`resume`**: Value used to resume after an [interrupt](/oss/python/langgraph/interrupts)

Use `Command` in three places:

- **[Return from nodes](#return-from-nodes)**: `update`, `goto`, and `graph`
- **[Input to `invoke` or `stream`](#input-to-invoke-or-stream)**: `resume` after an interrupt
- **[Return from tools](#return-from-tools)**: Updates and routing from inside a tool

### Return from nodes

#### `update` and `goto`

Return [`Command`](https://reference.langchain.com/python/langgraph/types/Command) to update state and route in one step:

```python
def my_node(state: State) -> Command[Literal["my_other_node"]]:
    return Command(
        update={"foo": "bar"},
        goto="my_other_node",
    )
```

Annotate the return type with the allowed destinations, for example `Command[Literal["my_other_node"]]`. That tells LangGraph which edges to render.

Use [`Command`](https://reference.langchain.com/python/langgraph/types/Command) when you need both an update and a route. For routing alone, prefer [conditional edges](#conditional-edges).




<Warning>
`Command` adds dynamic edges. Static edges from the same node still run. Do not mix `Command` routing with `add_edge` from one node.
</Warning>




See [Combine control flow and state updates with Command](/oss/python/langgraph/graph-api-send-command#combine-control-flow-and-state-updates-with-command).

#### `graph`

From a [subgraph](/oss/python/langgraph/use-subgraphs) node, navigate to a node in the parent graph with `Command.PARENT`:

```python
def my_node(state: State) -> Command[Literal["other_subgraph"]]:
    return Command(
        update={"foo": "bar"},
        goto="other_subgraph",  # where `other_subgraph` is a node in the parent graph
        graph=Command.PARENT,
    )
```




When you update a parent key that the subgraph also uses, define a [reducer](#reducers) for that key in the parent. See [Navigate to a node in a parent graph](/oss/python/langgraph/graph-api-send-command#navigate-to-a-node-in-a-parent-graph).

### Input to `invoke` or `stream`

<Warning>
`Command(resume=...)` is the only `Command` pattern meant as input to `invoke()` / `stream()` (optionally with `update=...` while resuming). Do not pass `Command(update=...)` alone to continue a conversation. Any `Command` as input resumes from the latest checkpoint, not from `START`, so a finished thread can look stuck. Pass a plain dict to start a new turn:

```python
# Wrong: resumes from the latest checkpoint
graph.invoke(Command(update={  # [!code --]
    "messages": [{"role": "user", "content": "follow up"}]  # [!code --]
}), config)  # [!code --]

# Correct: plain dict restarts from START
graph.invoke({  # [!code ++]
    "messages": [{"role": "user", "content": "follow up"}]  # [!code ++]
}, config)  # [!code ++]
```
</Warning>




#### `resume`

Use `Command(resume=...)` after an [interrupt](/oss/python/langgraph/interrupts). The value becomes the return value of the paused `interrupt()` call:

```python
graph.invoke(Command(resume="yes"), config)
```




See [Interrupts](/oss/python/langgraph/interrupts) for full patterns.

### Return from tools

Return [`Command`](https://reference.langchain.com/python/langgraph/types/Command) from tools to update state and route after the tool finishes. `goto` from a tool adds a dynamic edge. Static edges on the calling node still run, so do not mix both.

See [Use inside tools](/oss/python/langgraph/graph-api-send-command#use-inside-tools).

## Graph migrations

LangGraph can migrate graph definitions while a checkpointer stores thread state:

- Threads that have finished can accept full topology changes (add, remove, or rename nodes and edges).
- Interrupted threads support topology changes except renaming or removing a node the thread is about to enter.
- Adding or removing state keys is backwards and forwards compatible.
- Renamed state keys lose saved values in existing threads.
- Incompatible type changes on existing keys can break older thread state.

For logic changes that stay schema-compatible (for example, a new tool set), see [Business compatibility](/oss/python/langgraph/backward-compatibility#business-compatibility).

## Runtime context

Runtime context passes dependencies into nodes without putting them in graph state (for example, model name or a database handle).

```python
@dataclass
class ContextSchema:
    llm_provider: str = "openai"

graph = StateGraph(State, context_schema=ContextSchema)
graph.invoke(inputs, context={"llm_provider": "anthropic"})
```

```python
from langgraph.runtime import Runtime

def node_a(state: State, runtime: Runtime[ContextSchema]):
    llm = get_llm(runtime.context.llm_provider)
```




See [Add runtime configuration](/oss/python/langgraph/graph-api-nodes#add-runtime-configuration).

### Recursion limit

The recursion limit caps how many [super-steps](#graphs) one run may execute. When the limit is exceeded, LangGraph raises `GraphRecursionError`.

From version 1.0.6, the default is 1000 steps. Pass `recursion_limit` as a top-level config key (not under `configurable`):

```python
graph.invoke(inputs, config={"recursion_limit": 5}, context={"llm": "anthropic"})
```




### Accessing and handling the recursion counter

Nodes can read the current step from `config["metadata"]["langgraph_step"]`. Related keys include `langgraph_node`, `langgraph_triggers`, `langgraph_path`, and `langgraph_checkpoint_ns`.

To finish gracefully before the limit, use the managed `RemainingSteps` value and route to a completion path. Catching `GraphRecursionError` outside the graph is a fallback.




For examples, see [Access and handle the recursion counter](/oss/python/langgraph/graph-api-control-flow#access-and-handle-the-recursion-counter).

## Learn more

- [Use the graph API](/oss/python/langgraph/use-graph-api)
- [Define and update state](/oss/python/langgraph/graph-api-state)
- [Build sequences, branches, and loops](/oss/python/langgraph/graph-api-control-flow)
- [Use Send and Command](/oss/python/langgraph/graph-api-send-command)
- [Configure nodes](/oss/python/langgraph/graph-api-nodes)
- [Visualize your graph](/oss/python/langgraph/graph-api-visualize)
- [Functional API overview](/oss/python/langgraph/functional-api)

---

<div className="source-links">
<Callout icon="terminal-2">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Callout>
<Callout icon="edit">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/oss/langgraph/graph-api.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
</div>
