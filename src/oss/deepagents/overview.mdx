---
title: Deep Agents overview
sidebarTitle: Overview
description: Build agents that can plan, use subagents, and leverage file systems for complex tasks
---

The easiest way to start building agents and applications powered by LLMs—with built-in capabilities for task planning, file systems for context management, subagent-spawning, and long-term memory.
You can use deep agents for any task, including complex, multi-step tasks.

We think of `deepagents` as an ["agent harness"](/oss/concepts/products#agent-harnesses-like-the-deep-agents-sdk). It is the same core tool calling loop as other agent frameworks, but with built-in tools and capabilities.

<Tip>
  The [LangSmith Engine](/langsmith/engine) detects issues in your Deep Agents traces and proposes fixes. You can open a pull request with the proposed fix directly from the Engine tab.
</Tip>

:::python
[`deepagents`](https://pypi.org/project/deepagents/) is a standalone library built on top of [LangChain](/oss/langchain/)'s core building blocks for agents. It uses the [LangGraph](/oss/langgraph/) runtime for durable execution, streaming, human-in-the-loop, and other features.
:::

:::js
[`deepagents`](https://www.npmjs.com/package/deepagents) is a standalone library built on top of [LangChain](/oss/langchain/)'s core building blocks for agents and using [LangGraph](/oss/langgraph/)'s tooling for running agents in production.
:::

The [`deepagents` repository](https://github.com/langchain-ai/deepagents) contains:
- **Deep Agents SDK**: A package for building agents that can handle any task
- [**Deep Agents Code**](/oss/deepagents/code): A terminal coding agent built on the Deep Agents SDK
- [**ACP integration**](/oss/deepagents/acp): An Agent Client Protocol connector for using deep agents in code editors like Zed

[LangChain](/oss/langchain/) is the framework that provides the core building blocks for your agents.
To learn more about the differences between LangChain, LangGraph, and Deep Agents, see [Frameworks, runtimes, and harnesses](/oss/concepts/products). For a side-by-side comparison with Anthropic's harness, see [Deep Agents vs. Claude Agent SDK](/oss/deepagents/comparison).

## <Icon icon="wand" /> Create a deep agent

:::python
<Tabs>
    <Tab title="Google">
        ```python
        # pip install -qU deepagents langchain-google-genai
        from deepagents import create_deep_agent

        def get_weather(city: str) -> str:
            """Get weather for a given city."""
            return f"It's always sunny in {city}!"

        agent = create_deep_agent(
            model="google_genai:gemini-3.5-flash",
            tools=[get_weather],
            system_prompt="You are a helpful assistant",
        )

        # Run the agent
        agent.invoke(
            {"messages": [{"role": "user", "content": "what is the weather in sf"}]}
        )
        ```
    </Tab>
    <Tab title="OpenAI">
        ```python
        # pip install -qU deepagents langchain-openai
        from deepagents import create_deep_agent

        def get_weather(city: str) -> str:
            """Get weather for a given city."""
            return f"It's always sunny in {city}!"

        agent = create_deep_agent(
            model="openai:gpt-5.4",
            tools=[get_weather],
            system_prompt="You are a helpful assistant",
        )

        # Run the agent
        agent.invoke(
            {"messages": [{"role": "user", "content": "what is the weather in sf"}]}
        )
        ```
    </Tab>
    <Tab title="Anthropic">
        ```python
        # pip install -qU deepagents langchain-anthropic
        from deepagents import create_deep_agent

        def get_weather(city: str) -> str:
            """Get weather for a given city."""
            return f"It's always sunny in {city}!"

        agent = create_deep_agent(
            model="anthropic:claude-sonnet-4-6",
            tools=[get_weather],
            system_prompt="You are a helpful assistant",
        )

        # Run the agent
        agent.invoke(
            {"messages": [{"role": "user", "content": "what is the weather in sf"}]}
        )
        ```
    </Tab>
    <Tab title="OpenRouter">
        ```python
        # pip install -qU deepagents langchain-openrouter
        from deepagents import create_deep_agent

        def get_weather(city: str) -> str:
            """Get weather for a given city."""
            return f"It's always sunny in {city}!"

        agent = create_deep_agent(
            model="openrouter:anthropic/claude-sonnet-4-6",
            tools=[get_weather],
            system_prompt="You are a helpful assistant",
        )

        # Run the agent
        agent.invoke(
            {"messages": [{"role": "user", "content": "what is the weather in sf"}]}
        )
        ```
    </Tab>
    <Tab title="Fireworks">
        ```python
        # pip install -qU deepagents langchain-fireworks
        from deepagents import create_deep_agent

        def get_weather(city: str) -> str:
            """Get weather for a given city."""
            return f"It's always sunny in {city}!"

        agent = create_deep_agent(
            model="fireworks:accounts/fireworks/models/qwen3p5-397b-a17b",
            tools=[get_weather],
            system_prompt="You are a helpful assistant",
        )

        # Run the agent
        agent.invoke(
            {"messages": [{"role": "user", "content": "what is the weather in sf"}]}
        )
        ```
    </Tab>
    <Tab title="Baseten">
        ```python
        # pip install -qU deepagents langchain-baseten
        from deepagents import create_deep_agent

        def get_weather(city: str) -> str:
            """Get weather for a given city."""
            return f"It's always sunny in {city}!"

        agent = create_deep_agent(
            model="baseten:zai-org/GLM-5",
            tools=[get_weather],
            system_prompt="You are a helpful assistant",
        )

        # Run the agent
        agent.invoke(
            {"messages": [{"role": "user", "content": "what is the weather in sf"}]}
        )
        ```
    </Tab>
    <Tab title="Ollama">
        ```python
        # pip install -qU deepagents langchain-ollama
        from deepagents import create_deep_agent

        def get_weather(city: str) -> str:
            """Get weather for a given city."""
            return f"It's always sunny in {city}!"

        agent = create_deep_agent(
            model="ollama:devstral-2",
            tools=[get_weather],
            system_prompt="You are a helpful assistant",
        )

        # Run the agent
        agent.invoke(
            {"messages": [{"role": "user", "content": "what is the weather in sf"}]}
        )
        ```
    </Tab>
</Tabs>
:::

:::js
```ts
import * as z from "zod";
// npm install deepagents langchain @langchain/core
import { createDeepAgent } from "deepagents";
import { tool } from "langchain";

const getWeather = tool(
  ({ city }) => `It's always sunny in ${city}!`,
  {
    name: "get_weather",
    description: "Get the weather for a given city",
    schema: z.object({
      city: z.string(),
    }),
  },
);

const agent = createDeepAgent({
  tools: [getWeather],
  systemPrompt: "You are a helpful assistant",
});

console.log(
  await agent.invoke({
    messages: [{ role: "user", content: "What's the weather in Tokyo?" }],
  })
);
```
:::

See the [Quickstart](/oss/deepagents/quickstart/) and [Customization guide](/oss/deepagents/customization/) to get started building your own agents and applications with Deep Agents.

<Tip>
Trace requests, debug agent behavior, and evaluate outputs with [LangSmith](https://smith.langchain.com). Follow the [tracing quickstart](/langsmith/trace-with-langchain) to get set up. When ready for production, [deploy to LangSmith Cloud](/langsmith/deploy-to-cloud) for managed hosting.
</Tip>

## Core capabilities

Use the **Deep Agents SDK** to build agents that handle complex, multi-step tasks across **any [model provider](/oss/deepagents/models)**. The SDK ships with the following built-in capabilities:

<Card title="Planning and task decomposition" icon="timeline">
    A built-in [`write_todos`](/oss/langchain/middleware/built-in#to-do-list) tool lets agents break down complex tasks into discrete steps, track progress, and adapt plans as new information emerges.
</Card>
<Card title="Context management" icon="scissors">
    File system tools ([`ls`](/oss/deepagents/harness#virtual-filesystem-access), [`read_file`](/oss/deepagents/harness#virtual-filesystem-access), [`write_file`](/oss/deepagents/harness#virtual-filesystem-access), [`edit_file`](/oss/deepagents/harness#virtual-filesystem-access)) offload large context to storage to prevent context window overflow, and [summarization](/oss/deepagents/context-engineering#summarization) compacts older messages to keep agents effective across extended sessions.
</Card>
<Card title="Pluggable filesystem backends" icon="plug">
    Swap the virtual filesystem to fit your use case via [pluggable backends](/oss/deepagents/backends): in-memory state, local disk, LangGraph store for cross-thread persistence, [sandboxes](/oss/deepagents/sandboxes) (Modal, Daytona, Deno), composite routing, or your own custom backend with declarative [permission rules](/oss/deepagents/permissions) that restrict which files agents can read or write.
</Card>
<Card title="Shell execution" icon="terminal">
    With a [sandbox backend](/oss/deepagents/sandboxes), agents get an `execute` tool to run shell commands for tests, builds, git operations, and system tasks — isolated from your host system.
</Card>
<Card title="Interpreters" icon="code">
    Add an [interpreter](/oss/deepagents/interpreters) to run JavaScript in an in-memory runtime. Interpreters let agents compose tools programmatically, orchestrate subagents, and transform structured data without a full shell environment.
</Card>
<Card title="Subagent spawning" icon="users-group">
    A built-in `task` tool spawns general-purpose or specialized subagents for context isolation, keeping the main agent's context clean while going deep on specific subtasks.
</Card>
<Card title="Long-term memory" icon="database">
    Persist memory across threads and conversations using LangGraph's [Memory Store](/oss/langgraph/persistence#memory-store).
</Card>
<Card title="Filesystem permissions" icon="lock">
    Declare [permission rules](/oss/deepagents/permissions) that control which files and directories agents can read or write. Subagents can inherit or override the parent's rules.
</Card>
<Card title="Human-in-the-loop" icon="user-check">
    Configure [human approval](/oss/deepagents/human-in-the-loop) for sensitive tool operations using LangGraph's interrupt capabilities.
</Card>
<Card title="Skills" icon="puzzle">
    Extend agents with reusable [skills](/oss/deepagents/skills) that provide specialized workflows, domain knowledge, and custom instructions.
</Card>
<Card title="Smart defaults" icon="wand">
    Ships with opinionated system prompts that teach the model to plan before acting, verify work, and manage context. Customize or replace the defaults as needed.
</Card>

:::python
For building custom agents without these builtin capabilities, consider using LangChain's [`create_agent`](/oss/langchain/agents) or building a custom [LangGraph](/oss/langgraph/overview) workflow.
:::

:::js
For building custom agents without these builtin capabilities, consider using LangChain's [`createAgent`](/oss/langchain/agents) or building a custom [LangGraph](/oss/langgraph/overview) workflow.
:::

## Get started

:::python
<CardGroup cols={2}>
    <Card title="Quickstart" icon="rocket" href="/oss/deepagents/quickstart">
        Build your first deep agent
    </Card>
    <Card title="Customization" icon="adjustments" href="/oss/deepagents/customization">
        Learn about customization options
    </Card>
    <Card title="Models" icon="cpu" href="/oss/deepagents/models">
        Configure models and providers
    </Card>
    <Card title="Backends" icon="plug" href="/oss/deepagents/backends">
        Choose and configure pluggable filesystem backends
    </Card>
    <Card title="Sandboxes" icon="cube" href="/oss/deepagents/sandboxes">
        Execute code in isolated environments
    </Card>
    <Card title="Interpreters" icon="code" href="/oss/deepagents/interpreters">
        Compose tools and transform data in QuickJS
    </Card>
    <Card title="Permissions" icon="lock" href="/oss/deepagents/permissions">
        Control filesystem access with permission rules
    </Card>
    <Card title="Human-in-the-loop" icon="user-check" href="/oss/deepagents/human-in-the-loop">
        Configure approval for sensitive operations
    </Card>
    <Card title="Code" icon="terminal" href="/oss/deepagents/code/overview">
        Use Deep Agents Code
    </Card>
    <Card title="ACP" icon="plug-connected" href="/oss/deepagents/acp">
        Use deep agents in code editors via ACP
    </Card>
    <Card title="Reference" icon="external-link" href="https://reference.langchain.com/python/deepagents/">
        See the `deepagents` API reference
    </Card>
</CardGroup>
:::

:::js
<CardGroup cols={2}>
    <Card title="Quickstart" icon="rocket" href="/oss/deepagents/quickstart">
        Build your first deep agent
    </Card>
    <Card title="Customization" icon="adjustments" href="/oss/deepagents/customization">
        Learn about customization options
    </Card>
    <Card title="Models" icon="cpu" href="/oss/deepagents/models">
        Configure models and providers
    </Card>
    <Card title="Backends" icon="plug" href="/oss/deepagents/backends">
        Choose and configure pluggable filesystem backends
    </Card>
    <Card title="Interpreters" icon="code" href="/oss/deepagents/interpreters">
        Compose tools and transform data in QuickJS
    </Card>
    <Card title="Permissions" icon="lock" href="/oss/deepagents/permissions">
        Control filesystem access with permission rules
    </Card>
    <Card title="Human-in-the-loop" icon="user-check" href="/oss/deepagents/human-in-the-loop">
        Configure approval for sensitive operations
    </Card>
    <Card title="Code" icon="terminal" href="/oss/deepagents/code/overview">
        Use Deep Agents Code
    </Card>
    <Card title="Reference" icon="external-link" href="https://reference.langchain.com/javascript/modules/deepagents.html">
        See the `deepagents` API reference
    </Card>
</CardGroup>
:::
