---
title: "Duel Agents integration"
description: "Integrate with the Duel Agents chat model using LangChain Python."
---

This guide provides a quick overview for getting started with the Duel Agents [chat model](/oss/langchain/models). Duel Agents routes each prompt against multiple models and bills the cheapest answer that still wins, behind an OpenAI-compatible proxy.

## Overview

### Details

| Class | Package | Serializable | JS support | Downloads | Version |
| :--- | :--- | :---: | :---: | :---: | :---: |
| [ChatDuel](https://github.com/2aronS/Duel-Agents/tree/main/python/langchain-duel) | [langchain-duel](https://pypi.org/project/langchain-duel/) | ❌ | ❌ | ![PyPI - Downloads](https://img.shields.io/pypi/dm/langchain-duel?style=flat-square&label=%20) | ![PyPI - Version](https://img.shields.io/pypi/v/langchain-duel?style=flat-square&label=%20) |

### Features

| [Tool calling](/oss/langchain/tools) | [Structured output](/oss/langchain/structured-output/) | [Image input](/oss/langchain/messages#multimodal) | [Audio input](/oss/langchain/messages#multimodal) | [Video input](/oss/langchain/messages#multimodal) | [Token-level streaming](/oss/langchain/streaming/) | Native async | [Token usage](/oss/langchain/models#token-usage) | [Logprobs](/oss/langchain/models#log-probabilities) |
| :---: | :---: | :---: |  :---: | :---: | :---: | :---: | :---: | :---: |
| ✅ | ✅ | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ | ❌ |

---

## Setup

To access Duel Agents models, create an API key at [duelagents.com/dashboard/settings](https://duelagents.com/dashboard/settings) (an active subscription is required) and install the `langchain-duel` integration package.

### Credentials

```python Set API key icon="key"
import getpass
import os

if "DUEL_API_KEY" not in os.environ:
    os.environ["DUEL_API_KEY"] = getpass.getpass("Enter your Duel API key: ")
```

To enable automated <Tooltip tip="Log each step of a model's execution to debug and improve it">tracing</Tooltip> of your model calls, set your [LangSmith](/langsmith/home) API key:

```python Enable tracing icon="flask"
os.environ["LANGSMITH_API_KEY"] = getpass.getpass("Enter your LangSmith API key: ")
os.environ["LANGSMITH_TRACING"] = "true"
```

### Installation

The LangChain Duel Agents integration lives in the `langchain-duel` package:

<CodeGroup>
    ```python pip
    pip install -U langchain-duel
    ```
    ```python uv
    uv add langchain-duel
    ```
</CodeGroup>

---

## Instantiation

Now we can instantiate our model object and generate responses:

```python Initialize chat model icon="robot"
from langchain_duel import ChatDuel

model = ChatDuel(
    model="duel-auto",
    temperature=0,
    max_tokens=1024,
    max_retries=2,
    # Other params - see langchain-openai for the full list
)
```

`duel-auto` lets the router pick the model. You can also pass a specific model name (for example `"gpt-4o-mini"` or `"claude-3-5-haiku-latest"`) to route to that provider.

---

## Invocation

<CodeGroup>
    ```python Dictionary format icon="book"
    messages = [
        {"role": "system", "content": "You are a poetry expert"},
        {"role": "user", "content": "Write a haiku about spring"},
    ]
    response = model.invoke(messages)
    print(response)
    ```

    ```python Message objects icon="message"
    from langchain.messages import SystemMessage, HumanMessage

    messages = [
        SystemMessage("You are a poetry expert"),
        HumanMessage("Write a haiku about spring"),
    ]
    response = model.invoke(messages)
    ```
</CodeGroup>

```python Text content icon="cursor-text"
print(response.text)
```

<Tip>
    Full guides are available on [chat model invocation types](/oss/langchain/models#invocation), [message types](/oss/langchain/messages#message-types), and [content blocks](/oss/langchain/messages#standard-content-blocks).
</Tip>

## Chaining

We can [chain](/oss/langchain/overview) the model with a prompt template:

```python Chaining icon="link"
from langchain_core.prompts import ChatPromptTemplate

prompt = ChatPromptTemplate(
    [
        ("system", "You translate {input_language} to {output_language}."),
        ("human", "{input}"),
    ]
)

chain = prompt | model
chain.invoke(
    {
        "input_language": "English",
        "output_language": "French",
        "input": "I love programming.",
    }
)
```

---

## API reference

`ChatDuel` subclasses `langchain_openai.ChatOpenAI`, so it accepts the same parameters and methods. See the [ChatOpenAI API reference](https://reference.langchain.com/python/integrations/langchain_openai/chat_models/ChatOpenAI/) and the [langchain-duel source](https://github.com/2aronS/Duel-Agents/tree/main/python/langchain-duel).
