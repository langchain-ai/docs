---
title: "Asqav"
description: "AI compliance monitoring for LangChain via AsqavCallbackHandler."
---

>[Asqav](https://asqav.com) is an AI governance platform that monitors LangChain
>applications for policy compliance, safety violations, and audit trail requirements.
>It works as a drop-in callback handler with no changes to your chain logic.

## Installation and setup

```bash
pip install "asqav[langchain]"
```

Get an API key at [asqav.com](https://asqav.com).

## Usage

```python
from asqav.extras.langchain import AsqavCallbackHandler

handler = AsqavCallbackHandler(api_key="sk_...")
```

Pass the handler via the `callbacks` config key:

```python
chain.invoke(input, config={"callbacks": [handler]})
```

Or attach it to a model directly:

```python
from langchain_openai import ChatOpenAI

llm = ChatOpenAI(callbacks=[handler])
```

## Example

```python
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate
from asqav.extras.langchain import AsqavCallbackHandler

handler = AsqavCallbackHandler(api_key="sk_...")

prompt = ChatPromptTemplate.from_template("Answer the question: {question}")
llm = ChatOpenAI(model="gpt-4o-mini")

chain = prompt | llm

response = chain.invoke(
    {"question": "What is the capital of France?"},
    config={"callbacks": [handler]},
)
print(response.content)
```

## Resources

- [Documentation](https://asqav.com/docs/langchain)
- [GitHub](https://github.com/jagmarques/asqav-sdk)
- [PyPI](https://pypi.org/project/asqav/)
