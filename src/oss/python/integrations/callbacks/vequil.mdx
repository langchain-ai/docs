---
title: "Vequil"
description: "Reliability and observability for AI agents with LangChain."
---

[Vequil](https://vequil.com) is a reliability and observability layer for AI agent operators.
It detects anomalies in real time (logic loops, retry storms, cost spikes, blocked tool calls),
tracks spend by agent and tool, and generates shareable weekly report cards.

## Installation and Setup

```bash
pip install vequil
# or with the langchain extra:
pip install "vequil[langchain]"
```

Get your workspace key at [vequil.com/quickstart](https://vequil.com/quickstart).

## Usage

The `VequilCallbackHandler` is a drop-in `BaseCallbackHandler` that works with any
LangChain chain, agent, or LLM call.

```python
from vequil import VequilClient
from vequil.integrations.langchain import VequilCallbackHandler

vq = VequilClient(workspace_key="vk_ws_...")
handler = VequilCallbackHandler(vq, agent_id="my-chain")

result = chain.invoke(
    {"input": "What is the weather in SF?"},
    config={"callbacks": [handler]},
)
```

Every tool call, LLM invocation, and chain error is logged to your Vequil workspace automatically.

## What Gets Captured

| Event | Logged |
|---|---|
| `on_tool_start` / `on_tool_end` / `on_tool_error` | Yes |
| `on_llm_start` / `on_llm_end` | Yes |
| `on_chain_error` | Yes |
| Token usage | Yes |

## Anomaly Detection

Vequil applies deterministic rules to detect:

- **Logic loops** -- same tool called repeatedly in a session
- **Retry storms** -- agent hammering a failing endpoint
- **Cost spikes** -- single call exceeding spend baseline
- **Blocked actions** -- auth failures and permission denials

## Links

- [PyPI](https://pypi.org/project/vequil/)
- [GitHub](https://github.com/nxd914/vequil)
- [Quickstart](https://vequil.com/quickstart)
- [Console](https://vequil.com/dashboard.html)
