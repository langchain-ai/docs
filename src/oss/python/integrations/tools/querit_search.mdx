---
title: "Querit search integration"
description: "Integrate with the Querit search tool using LangChain Python."
---

> [!Highlight]
> **Real-time Information Engine**: Built for AI applications with sub-second latency for instant access to current web data
> **Massive Global Index**: Backed by a web index containing around 300 billion pages with coverage across major countries and languages
> **High Concurrency Support**: Ultra-high concurrency capabilities ensure enterprise-grade availability for production workloads
> **Precise & Trusted Results**: Industry-leading algorithms and real-time LLM understanding deliver high-quality, authoritative search results
> **Advanced Retrieval Capabilities**: Supports filtering by country and language for flexible customization tailored to business scenarios

This guide shows how to use the **WebSearchTool** from Querit Search API with LangChain.

## Benefits for LangChain Users

The langchain-querit plugin bridges the gap between LLMs and real-time web data:

- **Solves LLM Knowledge Gaps**: Access to around 300 billion web pages in real-time addresses LLM knowledge cutoff limitations and ensures responses contain current, accurate information
- **Sub-second Latency**: Ultra-low response times enable agents to retrieve and process information instantly without impacting user experience
- **Enterprise-Grade Reliability**: High concurrency support ensures consistent performance under production workloads with reliable availability
- **High-Quality Sources**: Industry-leading algorithms and LLM understanding deliver precise, authoritative results from trusted global sources
- **Tailored Retrieval**: Advanced filtering by country and language allows customization for specific business scenarios and regional requirements
- **Drop-in Integration**: Built on LangChain's BaseTool pattern for seamless integration with existing agents, chains, and workflows
- **Structured Output**: Consistently formatted SearchResult objects (title, link, snippet, display_link, position) for easy parsing and downstream use
- **Simple Setup**: Minimal configuration required - just install the package and set QUERIT_API_KEY environment variable
- **Developer-Friendly Pricing**: 1,000 free queries per month with immediate access upon registration and zero-barrier integration into your AI projects
- **International Coverage**: Ideal choice for non-English developers with native support for 10 languages including Spanish, Portuguese, and more

## Installation

First, install the Querit search package:

```bash
pip install langchain-querit
```

## Basic Usage

Set your API key as an environment variable:

```python
import os
os.environ["QUERIT_API_KEY"] = "your-querit-api-key"
```

Create a search tool:

```python
from langchain.tools import tool
from langchain_websearch import WebSearchTool

search_tool = WebSearchTool()

@tool
def querit_search(query: str) -> str:
    """Search Querit for recent results."""
    return search_tool._run(query)
```

Test the tool:

```python
querit_search.invoke("latest Python programming news")
```

## Advanced Configuration

### Configuring Results Count

```python
# Configure to return specific number of results
search_tool = WebSearchTool(num_results=5)

@tool
def querit_search_limited(query: str) -> str:
    """Search Querit and return up to 5 results."""
    return search_tool._run(query)
```

### Using in LangChain Agents

```python
from langchain.agents import initialize_agent, AgentType
from langchain_openai import ChatOpenAI
from langchain_websearch import WebSearchTool

# Initialize LLM
llm = ChatOpenAI(temperature=0)

# Create web search tool
search_tool = WebSearchTool(name="web_search", description="Search the web for information")

# Initialize agent
agent = initialize_agent(
    tools=[search_tool],
    llm=llm,
    agent=AgentType.ZERO_SHOT_REACT_DESCRIPTION,
    verbose=True
)

# Run the agent
agent.run("What are the latest developments in AI research?")
```

### Tool Registration with LangChain

```python
from langchain.agents import load_tools
from langchain_websearch import WebSearchTool

# Create and register the tool
search_tool = WebSearchTool()

# Use with langchain tools
tools = load_tools(["python_repl", "requests"], llm=llm)
tools.append(search_tool)
```

## Configuration Options

| Parameter | Description | Default | Range |
| ----------- | ------------- | --------- | ------- |
| `num_results` | Number of search results to return | 10 | 1-50 |
| `region` | Search region/language | "en-US" | - |
| `safe_search` | Enable safe search filtering | True | bool |

## Example: Complete Agent Setup

```python
import os
from langchain.agents import create_agent, AgentExecutor
from langchain_openai import ChatOpenAI
from langchain_websearch import WebSearchTool
from langchain.agents import Tool

# Set API keys
os.environ["QUERIT_API_KEY"] = "your-querit-api-key"
os.environ["OPENAI_API_KEY"] = "your-openai-api-key"

# Initialize components
llm = ChatOpenAI(temperature=0)
web_search = WebSearchTool(num_results=5)

# Create tools
tools = [
    Tool(
        name="Web Search",
        func=web_search._run,
        description="Search the web for current information"
    )
]

# Create agent
agent = create_agent(
    llm=llm,
    tools=tools,
    verbose=True
)

# Execute query
agent.invoke("Find recent news about quantum computing")
```
