import SmithdbExperimentRunsQueryBasicBeforePy from '/snippets/code-samples/smithdb-migration/experiment-runs-query-basic-before-py.mdx';
import SmithdbExperimentRunsQueryBasicAfterPy from '/snippets/code-samples/smithdb-migration/experiment-runs-query-basic-after-py.mdx';
import SmithdbExperimentRunsQueryBasicBeforeJs from '/snippets/code-samples/smithdb-migration/experiment-runs-query-basic-before-js.mdx';
import SmithdbExperimentRunsQueryBasicAfterJs from '/snippets/code-samples/smithdb-migration/experiment-runs-query-basic-after-js.mdx';
import SmithdbExperimentRunsQueryBasicBeforeKt from '/snippets/code-samples/smithdb-migration/experiment-runs-query-basic-before-kt.mdx';
import SmithdbExperimentRunsQueryBasicAfterKt from '/snippets/code-samples/smithdb-migration/experiment-runs-query-basic-after-kt.mdx';
import SmithdbExperimentRunsQueryBasicBeforeGo from '/snippets/code-samples/smithdb-migration/experiment-runs-query-basic-before-go.mdx';
import SmithdbExperimentRunsQueryBasicAfterGo from '/snippets/code-samples/smithdb-migration/experiment-runs-query-basic-after-go.mdx';
import SmithdbExperimentRunsQueryBasicBeforeSh from '/snippets/code-samples/smithdb-migration/experiment-runs-query-basic-before-sh.mdx';
import SmithdbExperimentRunsQueryBasicAfterSh from '/snippets/code-samples/smithdb-migration/experiment-runs-query-basic-after-sh.mdx';
import SmithdbExperimentRunsQueryPaginationBeforeSh from '/snippets/code-samples/smithdb-migration/experiment-runs-query-pagination-before-sh.mdx';
import SmithdbExperimentRunsQueryPaginationAfterSh from '/snippets/code-samples/smithdb-migration/experiment-runs-query-pagination-after-sh.mdx';
import SmithdbExperimentRunsQuerySortBeforeSh from '/snippets/code-samples/smithdb-migration/experiment-runs-query-sort-before-sh.mdx';
import SmithdbExperimentRunsQuerySortAfterSh from '/snippets/code-samples/smithdb-migration/experiment-runs-query-sort-after-sh.mdx';

## Dataset experiment runs: query

Query dataset examples with the experiment runs associated with each example. Returns a cursor-paginated page for comparing one or more experiments.

### Main changes

#### Method name

<Tabs>
  <Tab title="Python">
    | Before | After |
    |--------|-------|
    | `client.get_experiment_results()` | `client.datasets.experiment_runs.query()` |
  </Tab>
  <Tab title="TypeScript">
    | Before | After |
    |--------|-------|
    | *(no public v1 Client method)* | `client.datasets.experimentRuns.query()` |
  </Tab>
  <Tab title="Java">
    | Before | After |
    |--------|-------|
    | `client.datasets().runs().create()` | `client.datasets().experimentRuns().query()` |
  </Tab>
  <Tab title="Go">
    | Before | After |
    |--------|-------|
    | `client.Datasets.Runs.New()` | `client.Datasets.ExperimentRuns.Query()` |
  </Tab>
  <Tab title="cURL">
    | Before | After |
    |--------|-------|
    | `POST /api/v1/datasets/{dataset_id}/runs` | `POST /v2/datasets/{dataset_id}/experiment-runs` |
  </Tab>
</Tabs>

#### Query parameters

<Tabs>
  <Tab title="Python">
    <Warning>
    `experiment_ids` is required and replaces `session_ids`. Values are still experiment tracing-project UUIDs.
    </Warning>

    | Before (`get_experiment_results`) | After (`datasets.experiment_runs.query`) | Notes |
    |---|---|---|
    | Experiment project ID | `experiment_ids` | `get_experiment_results` accepted one project/experiment; v2 accepts a required non-empty list |
    | `limit` | `page_size` | `page_size` defaults to 20, max 100 |
    | *(handled internally)* | `cursor` | Pass the previous page's `next_cursor` to fetch the next page |
    | `preview` | `selects` | Omitted `selects` returns only run IDs; use `INPUTS_PREVIEW` and `OUTPUTS_PREVIEW` for previews, or `INPUTS` and `OUTPUTS` for full payloads |
    | *(not exposed)* | `sort` | Use `{by, order}` for feedback-score sorting |
    | `filters` | `filters` | Unchanged; maps experiment UUID strings to filter expressions |
    | `comparative_experiment_id` | `comparative_experiment_id` | Unchanged |
    | *(not exposed)* | `example_ids` | Optional example UUID filter, max 1000 |
    | *(not exposed)* | *(removed)* | The v2 endpoint returns JSON only |
    | *(not exposed)* | *(removed)* | No v2 JSON equivalent |
  </Tab>
  <Tab title="TypeScript">
    <Warning>
    `experiment_ids` is required and replaces `session_ids`. Values are still experiment tracing-project UUIDs.
    </Warning>

    | Before | After (`datasets.experimentRuns.query`) | Notes |
    |---|---|---|
    | *(no public v1 Client method)* | `experiment_ids` | Required and non-empty |
    | *(no public v1 Client method)* | `page_size` | Defaults to 20, max 100 |
    | *(no public v1 Client method)* | `cursor` | Pass the previous page's `next_cursor` instead of a numeric offset |
    | *(no public v1 Client method)* | `selects` | Omitted `selects` returns only run IDs; use `INPUTS_PREVIEW` and `OUTPUTS_PREVIEW` for previews, or `INPUTS` and `OUTPUTS` for full payloads |
    | *(no public v1 Client method)* | `sort` | Use `{ by, order }` for feedback-score sorting |
    | *(no public v1 Client method)* | `filters` | Maps experiment UUID strings to filter expressions |
    | *(no public v1 Client method)* | `comparative_experiment_id` | Scopes pairwise-annotation feedback |
    | *(no public v1 Client method)* | `example_ids` | Optional example UUID filter, max 1000 |
    | *(no public v1 Client method)* | *(removed)* | The v2 endpoint returns JSON only |
    | *(no public v1 Client method)* | *(removed)* | No v2 JSON equivalent |
  </Tab>
  <Tab title="Java">
    <Warning>
    `experimentIds()` is required and replaces `sessionIds()`. Values are still experiment tracing-project UUIDs.
    </Warning>

    | Before (`RunCreateParams`) | After (`ExperimentRunQueryParams`) | Notes |
    |---|---|---|
    | `sessionIds()` | `experimentIds()` | Renamed; required and non-empty |
    | `limit()` | `pageSize()` | Defaults to 20, max 100 |
    | `offset()` | `cursor()` | Pass the previous page's `nextCursor()` instead of a numeric offset |
    | `preview()` | `selects()` | Omitted selects return only run IDs; add `Select.INPUTS_PREVIEW` and `Select.OUTPUTS_PREVIEW` for previews |
    | `sortParams()` | `sort()` | Shape changed from `sortBy()` / `sortOrder()` to `by()` / `order()` |
    | `filters()` | `filters()` | Unchanged |
    | `comparativeExperimentId()` | `comparativeExperimentId()` | Unchanged |
    | `exampleIds()` | `exampleIds()` | Unchanged, max 1000 |
    | `format()` | *(removed)* | The v2 endpoint returns JSON only |
    | `includeAnnotatorDetail()` | *(removed)* | No v2 JSON equivalent |
  </Tab>
  <Tab title="Go">
    <Warning>
    `ExperimentIDs` is required and replaces `SessionIDs`. Values are still experiment tracing-project UUIDs.
    </Warning>

    | Before (`DatasetRunNewParams`) | After (`DatasetExperimentRunQueryParams`) | Notes |
    |---|---|---|
    | `SessionIDs` | `ExperimentIDs` | Renamed; required and non-empty |
    | `Limit` | `PageSize` | Defaults to 20, max 100 |
    | `Offset` | `Cursor` | Pass the previous page's `NextCursor` instead of a numeric offset |
    | `Preview` | `Selects` | Omitted selects return only run IDs; use `InputsPreview` and `OutputsPreview` select constants for previews |
    | `SortParams` | `Sort` | Shape changed from `SortBy` / `SortOrder` to `By` / `Order` |
    | `Filters` | `Filters` | Unchanged |
    | `ComparativeExperimentID` | `ComparativeExperimentID` | Unchanged |
    | `ExampleIDs` | `ExampleIDs` | Unchanged, max 1000 |
    | `Format` | *(removed)* | The v2 endpoint returns JSON only |
    | `IncludeAnnotatorDetail` | *(removed)* | No v2 JSON equivalent |
  </Tab>
  <Tab title="cURL">
    <Warning>
    `experiment_ids` is required and replaces `session_ids`. Values are still experiment tracing-project UUIDs.
    </Warning>

    | Before (`POST /api/v1/datasets/{dataset_id}/runs` body) | After (`POST /v2/datasets/{dataset_id}/experiment-runs` body) | Notes |
    |---|---|---|
    | `session_ids` | `experiment_ids` | Renamed; required and non-empty |
    | `limit` | `page_size` | Defaults to 20, max 100 |
    | `offset` | `cursor` | Pass the previous page's `next_cursor` instead of a numeric offset |
    | `preview` | `selects` | Omitted `selects` returns only run IDs; use `INPUTS_PREVIEW` and `OUTPUTS_PREVIEW` for previews, or `INPUTS` and `OUTPUTS` for full payloads |
    | `sort_params` | `sort` | Shape changed from `{sort_by, sort_order}` to `{by, order}` |
    | `filters` | `filters` | Unchanged; maps experiment UUID strings to filter expressions |
    | `comparative_experiment_id` | `comparative_experiment_id` | Unchanged |
    | `example_ids` | `example_ids` | Unchanged, max 1000 |
    | `format=csv` | *(removed)* | The v2 endpoint returns JSON only |
    | `include_annotator_detail` | *(removed)* | No v2 JSON equivalent |
  </Tab>
</Tabs>

#### Response fields

<Tabs>
  <Tab title="Python">
    `get_experiment_results` returned experiment results with an `examples_with_runs` iterator. `datasets.experiment_runs.query` returns a paginated page object with `items` and `next_cursor`.

    | Before | After | Notes |
    |---|---|---|
    | `examples_with_runs` | `page.items` | Example rows moved into the page envelope |
    | *(handled internally)* | `page.next_cursor` | Pass as `cursor` to fetch the next page |
    | Run fields controlled by `preview=True/False` | Run fields controlled by `selects=[...]` | Omitted selects return only `id` |
    | `run.session_id` | `run.project_id` | Renamed |
    | `run.in_dataset` | `run.is_in_dataset` | Renamed |
    | Cost detail strings | Numeric cost fields | v2 returns numeric costs |
  </Tab>
  <Tab title="TypeScript">
    The legacy dataset runs endpoint was not exposed on the public TypeScript Client. `datasets.experimentRuns.query` returns a paginated page with item helpers and a `next_cursor` value.

    | Before | After | Notes |
    |---|---|---|
    | Legacy endpoint JSON array | `page.getPaginatedItems()` | Example rows moved into the page envelope |
    | Numeric `offset` in direct HTTP requests | `page.next_cursor` | Pass as `cursor` to fetch the next page |
    | Run fields controlled by `preview` in direct HTTP requests | Run fields controlled by `selects: [...]` | Omitted selects return only `id` |
    | `run.session_id` | `run.project_id` | Renamed |
    | `run.in_dataset` | `run.is_in_dataset` | Renamed |
    | Cost detail strings | Numeric cost fields | v2 returns numeric costs |
  </Tab>
  <Tab title="Java">
    `runs().create` returned an optional list. `experimentRuns().query` returns a page object with `items()` and `nextCursor()`.

    | Before | After | Notes |
    |---|---|---|
    | `Optional<List<ExampleWithRunsCh>>` | `ExperimentRunQueryPage` | Example rows moved into the page envelope |
    | Numeric `offset()` for the next request | `nextCursor()` | Pass as `cursor()` to fetch the next page |
    | Run fields controlled by `preview()` | Run fields controlled by `selects()` | Omitted selects return only `id` |
    | `sessionId()` | `projectId()` | Renamed |
    | `inDataset()` | `isInDataset()` | Renamed |
    | Cost detail strings | Numeric cost fields | v2 returns numeric costs |
  </Tab>
  <Tab title="Go">
    `Datasets.Runs.New` returned a slice pointer. `Datasets.ExperimentRuns.Query` returns an `ItemsCursorPostPagination` with `Items` and `NextCursor`.

    | Before | After | Notes |
    |---|---|---|
    | `*[]ExampleWithRunsCh` | `*pagination.ItemsCursorPostPagination[DatasetExperimentRunQueryResponse]` | Example rows moved into the page envelope |
    | Numeric `Offset` for the next request | `NextCursor` | Pass as `Cursor` to fetch the next page |
    | Run fields controlled by `Preview` | Run fields controlled by `Selects` | Omitted selects return only `ID` |
    | `SessionID` | `ProjectID` | Renamed |
    | `InDataset` | `IsInDataset` | Renamed |
    | Cost detail strings | Numeric cost fields | v2 returns numeric costs |
  </Tab>
  <Tab title="cURL">
    `POST /api/v1/datasets/{dataset_id}/runs` returned a JSON array. `POST /v2/datasets/{dataset_id}/experiment-runs` returns `{ "items": [...], "next_cursor": "..." }`.

    | Before | After | Notes |
    |---|---|---|
    | Top-level array | `items` | Example rows moved into the response envelope |
    | Numeric `offset` for the next request | `next_cursor` | Pass as `cursor` to fetch the next page |
    | Run fields controlled by `preview` | Run fields controlled by `selects` | Omitted selects return only `id` |
    | `session_id` | `project_id` | Renamed |
    | `in_dataset` | `is_in_dataset` | Renamed |
    | Cost detail strings | Numeric cost fields | v2 returns numeric costs |
  </Tab>
</Tabs>

### Examples

#### Query experiment runs and request preview fields

<Tabs>
  <Tab title="Python">
    `get_experiment_results(..., preview=True)` is replaced by explicit `selects`. Include preview select fields to keep the lightweight response shape.

<Tabs sync={false}>
  <Tab title="Before">
    <SmithdbExperimentRunsQueryBasicBeforePy />
  </Tab>
  <Tab title="After">
    <SmithdbExperimentRunsQueryBasicAfterPy />
  </Tab>
</Tabs>

  </Tab>
  <Tab title="TypeScript">
    The legacy endpoint was not exposed on the public TypeScript Client. Use explicit `selects` in the v2 SDK method to keep the lightweight response shape.

<Tabs sync={false}>
  <Tab title="Before">
    <SmithdbExperimentRunsQueryBasicBeforeJs />
  </Tab>
  <Tab title="After">
    <SmithdbExperimentRunsQueryBasicAfterJs />
  </Tab>
</Tabs>

  </Tab>
  <Tab title="Java">
    `preview(true)` is replaced by explicit `selects`. Include preview select fields to keep the lightweight response shape.

<Tabs sync={false}>
  <Tab title="Before">
    <SmithdbExperimentRunsQueryBasicBeforeKt />
  </Tab>
  <Tab title="After">
    <SmithdbExperimentRunsQueryBasicAfterKt />
  </Tab>
</Tabs>

  </Tab>
  <Tab title="Go">
    `Preview: true` is replaced by explicit `Selects`. Include preview select fields to keep the lightweight response shape.

<Tabs sync={false}>
  <Tab title="Before">
    <SmithdbExperimentRunsQueryBasicBeforeGo />
  </Tab>
  <Tab title="After">
    <SmithdbExperimentRunsQueryBasicAfterGo />
  </Tab>
</Tabs>

  </Tab>
  <Tab title="cURL">
    `preview: true` is replaced by explicit `selects`. Include preview select fields to keep the lightweight response shape.

<Tabs sync={false}>
  <Tab title="Before">
    <SmithdbExperimentRunsQueryBasicBeforeSh />
  </Tab>
  <Tab title="After">
    <SmithdbExperimentRunsQueryBasicAfterSh />
  </Tab>
</Tabs>

  </Tab>
</Tabs>

#### Page with a cursor

The v1 endpoint used numeric offsets. The v2 endpoint uses the opaque `next_cursor` from the previous response.

<Tabs sync={false}>
  <Tab title="Before">
    <SmithdbExperimentRunsQueryPaginationBeforeSh />
  </Tab>
  <Tab title="After">
    <SmithdbExperimentRunsQueryPaginationAfterSh />
  </Tab>
</Tabs>

#### Sort by feedback score

The v1 endpoint used `sort_params.sort_by` and `sort_params.sort_order`. The v2 endpoint uses `sort.by` and `sort.order`, and sorting is supported only when you query one experiment.

<Tabs sync={false}>
  <Tab title="Before">
    <SmithdbExperimentRunsQuerySortBeforeSh />
  </Tab>
  <Tab title="After">
    <SmithdbExperimentRunsQuerySortAfterSh />
  </Tab>
</Tabs>
