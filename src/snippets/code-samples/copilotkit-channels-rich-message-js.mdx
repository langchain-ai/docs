```tsx icon="message" title="bot.ts"
import { Header, Message, Section } from "@copilotkit/channels";

channel.onMention(async ({ thread }) => {
  await thread.post(
    <Message accent="#006DDD">
      <Header>On it</Header>
      <Section>Looking into that now, one moment…</Section>
    </Message>,
  );

  await thread.runAgent();
});
```
