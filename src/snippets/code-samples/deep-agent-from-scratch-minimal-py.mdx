<CodeGroup>
    ```python Google
    from langchain.agents import create_agent

    agent = create_agent("google_genai:gemini-3.6-flash", tools=[])
    ```

    ```python OpenAI
    from langchain.agents import create_agent

    agent = create_agent("openai:gpt-5.5", tools=[])
    ```

    ```python Anthropic
    from langchain.agents import create_agent

    agent = create_agent("anthropic:claude-sonnet-4-6", tools=[])
    ```

    ```python OpenRouter
    from langchain.agents import create_agent

    agent = create_agent("openrouter:z-ai/glm-5.2", tools=[])
    ```

    ```python Fireworks
    from langchain.agents import create_agent

    agent = create_agent("fireworks:accounts/fireworks/models/glm-5p2", tools=[])
    ```

    ```python Baseten
    from langchain.agents import create_agent

    agent = create_agent("baseten:zai-org/GLM-5.2", tools=[])
    ```

    ```python Ollama
    from langchain.agents import create_agent

    agent = create_agent("ollama:north-mini-code-1.0", tools=[])
    ```
</CodeGroup>
