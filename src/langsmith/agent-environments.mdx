---
title: Agent environments
sidebarTitle: Environments
description: How an agent's environments divide its traces, how to select one, and which parts of the set are fixed.
icon: "stack"
---

import AgentBetaNote from '/snippets/langsmith/agent-beta-note.mdx';

An _environment_ records where an agent was running when it produced a trace. Every trace an [agent](/langsmith/agents) records lands in exactly one of its environments, which are drawn from a fixed set of four.

Because the environment travels with the trace, where the agent was running is a property of the trace itself.

<AgentBetaNote />

## The four environments

An agent's environments come from a fixed set of four, and custom names are not supported. Listed most production-like first, they are Production, Staging, Development, and Local, which is the order the environment switcher uses.

How many of the four an agent has depends on how it was created. An agent created in the UI, through [Build](/langsmith/build-an-agent) or Fleet, starts with **Production** alone, and each of the others appears the first time a trace is addressed to it. An agent created any other way, including one created automatically by tracing to an identifier that does not exist yet, starts with all four.

LangSmith attaches no behavior to the choice beyond keeping each environment's traces apart. An environment does not change how an agent is instrumented, and the same build can report into a different environment by changing its [agent addressing](/langsmith/log-traces-to-agent).

## Select an environment

Wherever LangSmith needs one environment instead of all of them, it uses the same control: a button showing the current environment, which opens the list. It lists the agent's environments in the order above, most production-like first, and defaults to **Production**. On deployments the list drops **Local**.

The list holds the environments that exist for the agent, so an agent created in the UI shows **Production** alone until something reports elsewhere.

This control drives environment selection on tracing, dashboards, alerts, [Engine](/langsmith/engine-overview), Studio, and deployments. [Evaluators](/langsmith/evaluators) and [Insights](/langsmith/insights) also scope to a single environment, but they do it without this dropdown.

{/*
  DOC-1597. The surface list started as a coverage claim taken from the component tree
  rather than from a capture. An earlier version named two surfaces that do not use this
  control and missed four that do. Do not add Evaluators or Insights to the list.

  All six are now confirmed by capture. Tracing, dashboards, alerts, Engine, and
  deployments on 2026-09-20, Studio on 2026-09-21. Nothing in this list rests on the
  component tree.

  Three of them place the control differently. On Engine it sits under a "Choose an
  environment to analyze" heading marked Required rather than in a toolbar. On Studio
  it sits in a second toolbar row beside the deployment name and the assistant picker,
  and it appears only once Studio reaches an Agent Server: with an unreachable one the
  screen is a connection error and a "Configure connection" button, with no control at
  all. And on deployments the list omits Local.
  Background in DOC-1597.
*/}

## See also

- [Agents](/langsmith/agents)
- [Log traces to an agent](/langsmith/log-traces-to-agent)
- [Log traces to a specific project](/langsmith/log-traces-to-project)
- [Observability concepts](/langsmith/observability-concepts)
