---
title: Set up the LLM auth proxy
sidebarTitle: LLM auth proxy
description: Deploy an Envoy-based proxy that authenticates and routes LLM requests from LangSmith to your upstream provider or gateway.
---

The LLM auth proxy is an Envoy-based reverse proxy that sits between LangSmith and your upstream LLM provider or gateway. It validates LangSmith-signed JWTs on every request and optionally injects authentication headers or transforms request and response bodies before forwarding upstream. This page explains how to set up the LLM auth proxy for a [self-hosted LangSmith instance](/langsmith/self-hosted).

<Info>
Self-hosted LangSmith is an add-on to the Enterprise plan. For more details, refer to [Pricing](https://www.langchain.com/pricing). [Contact our sales team](https://www.langchain.com/contact-sales) if you want to get a license key to trial LangSmith in your environment.
</Info>

Use the LLM auth proxy when you need to:

- Authenticate LLM requests from the LangSmith Playground or LLM-as-judge evaluations against your own provider gateway.
- Inject provider-specific API keys or auth headers without exposing them to end users.
- Transform request or response bodies (for example, converting between OpenAI format and a custom gateway format).

## Prerequisites

1. A running [self-hosted LangSmith instance on Kubernetes](/langsmith/kubernetes).
1. An upstream LLM provider or gateway URL (for example, `https://gateway.example.com`).
1. Helm 3 installed.

<Note>
The LLM auth proxy is deployed as a separate Helm chart (`langsmith-auth-proxy`) alongside your main LangSmith installation. For Cloud Run deployments, see the [Cloud Run auth proxy repository](https://github.com/langchain-ai/cloud-run-auth-proxy).
</Note>

## Architecture

The proxy processes each request through the following pipeline:

```
Client → Envoy (:10000)
  → Health check filter (/healthz bypasses auth)
  → JWT validation (RS256, configurable issuer + audiences)
  → [optional] ext_authz HTTP filter (inject provider API key)
  → [optional] ext_proc gRPC filter (transform request/response bodies)
  → Upstream LLM provider or gateway
```

The proxy is provider-agnostic. The only provider-specific piece is the ext_authz service, which you configure per provider to inject the appropriate authentication headers.

## Install the Helm chart

Add the LangChain Helm repository and install the auth proxy chart:

```bash
helm repo add langchain https://langchain-ai.github.io/helm/
helm repo update
helm install langsmith-auth-proxy langchain/langsmith-auth-proxy \
  --namespace <your-namespace> \
  -f values.yaml
```

## Configure JWT validation

At minimum, configure the upstream URL and JWT validation in your `values.yaml`.

```yaml
authProxy:
  upstream: "https://gateway.example.com"
  jwtIssuer: "langsmith"
  jwtAudiences:
    - "my-audience"
  jwksUri: "https://langsmith.example.com/.well-known/jwks.json"
```

| Parameter | Default | Description |
|-----------|---------|-------------|
| `upstream` | `""` | URL of your LLM provider or gateway. If a path is included, all requests are prefixed with it (for example, `/chat/completions` becomes `/api/v1/chat/completions`). |
| `jwtIssuer` | `"langsmith"` | Expected `iss` claim in the JWT. |
| `jwtAudiences` | `[]` | List of expected `aud` claims. Must match the audiences in the signed JWT. |
| `jwksUri` | `""` | Remote JWKS endpoint for fetching public keys. For self-hosted LangSmith, use `https://<your-langsmith-host>/.well-known/jwks.json`. For SaaS, use `https://api.smith.langchain.com/.well-known/jwks.json`. |
| `jwksJson` | `""` | Inline JWKS JSON string. Mutually exclusive with `jwksUri`. If both are set, `jwksUri` takes precedence. |
| `jwksCacheDurationSeconds` | `300` | Cache duration in seconds for remote JWKS keys. Only used when `jwksUri` is set. |
| `streamIdleTimeout` | `"300s"` | Idle timeout for streaming responses (such as SSE from LLM providers). |

To disable JWT validation for testing or when validation is handled elsewhere:

```yaml
authProxy:
  jwtValidation:
    enabled: false
```

## Set up external authorization (ext_authz)

Use the ext_authz integration to inject authentication headers (such as API keys) before requests are forwarded upstream. This uses Envoy's [HTTP ext_authz filter](https://www.envoyproxy.io/docs/envoy/latest/configuration/http/http_filters/ext_authz_filter).

When enabled, Envoy calls your ext_authz service at `/check` before forwarding each request. The service receives the `x-langsmith-llm-auth` header containing the validated JWT and can inject or override headers like `Authorization`.

```yaml
authProxy:
  extAuthz:
    enabled: true
    serviceUrl: "http://my-auth-service:8080"
    timeout: "10s"
```

### ext_authz interface

**Request** — Envoy sends an HTTP request to `{serviceUrl}/check{original_path}` with:

- The same HTTP method as the original request.
- Headers matching `allowedHeadersRegex` patterns (defaults to all headers).
- The request body only if `sendBody: true`.

**Response** — Your service returns a plain HTTP response:

- **`2xx`**: Allow the request. Headers matching `allowedUpstreamHeaders` patterns (such as `authorization` and `x-*` prefixed headers) are forwarded upstream.
- **Non-`2xx`**: Deny the request. The status code and headers matching `allowedClientHeaders` are sent back to the client.

### ext_authz parameters

| Parameter | Default | Description |
|-----------|---------|-------------|
| `serviceUrl` | `""` | HTTP URL of your ext_authz service. |
| `timeout` | `"10s"` | Timeout for ext_authz requests. |
| `allowedHeadersRegex` | `".*"` | RE2 regex controlling which client request headers are forwarded to the ext_authz service. |
| `disallowedHeadersRegex` | `""` | RE2 regex for headers to exclude (takes precedence over `allowedHeadersRegex`). |
| `allowedUpstreamHeaders` | `[{exact: "authorization"}, {prefix: "x-"}]` | Patterns controlling which ext_authz response headers are forwarded upstream. Each entry is an object with one of: `exact`, `prefix`, or `safe_regex`. |
| `sendBody` | `false` | Whether to send the request body to ext_authz. |
| `maxRequestBytes` | `8192` | Maximum request body bytes to buffer for ext_authz. |
| `headersToAdd` | `[]` | Static headers to add to every check request. Example: `[{key: "x-auth-context", value: "langsmith"}]` |

### Write an ext_authz service

The ext_authz service is the only provider-specific component. It receives every authorized request from Envoy and returns headers to inject before forwarding upstream.

<Accordion title="Example: Anthropic ext_authz service">
```python
from http.server import HTTPServer, BaseHTTPRequestHandler
from socketserver import ThreadingMixIn
import os

API_KEY = os.environ["LLM_PROVIDER_API_KEY"]

class ThreadedHTTPServer(ThreadingMixIn, HTTPServer):
    daemon_threads = True

class Handler(BaseHTTPRequestHandler):
    def do_any(self):
        self.send_response(200)
        self.send_header("x-api-key", API_KEY)
        self.send_header("anthropic-version", "2023-06-01")
        self.send_header("x-envoy-auth-headers-to-remove", "x-langsmith-llm-auth")
        self.end_headers()

    do_GET = do_POST = do_PUT = do_DELETE = do_PATCH = do_HEAD = do_OPTIONS = do_any

    def log_message(self, format, *args):
        print(f"ext_authz: {args[0]}", flush=True)

if __name__ == "__main__":
    server = ThreadedHTTPServer(("0.0.0.0", 10002), Handler)
    print("ext_authz listening on :10002", flush=True)
    server.serve_forever()
```
</Accordion>

<Accordion title="Example: OpenAI ext_authz service">
```python
from http.server import HTTPServer, BaseHTTPRequestHandler
from socketserver import ThreadingMixIn
import os

API_KEY = os.environ["LLM_PROVIDER_API_KEY"]

class ThreadedHTTPServer(ThreadingMixIn, HTTPServer):
    daemon_threads = True

class Handler(BaseHTTPRequestHandler):
    def do_any(self):
        self.send_response(200)
        self.send_header("Authorization", f"Bearer {API_KEY}")
        self.send_header("x-envoy-auth-headers-to-remove", "x-langsmith-llm-auth")
        self.end_headers()

    do_GET = do_POST = do_PUT = do_DELETE = do_PATCH = do_HEAD = do_OPTIONS = do_any

    def log_message(self, format, *args):
        print(f"ext_authz: {args[0]}", flush=True)

if __name__ == "__main__":
    server = ThreadedHTTPServer(("0.0.0.0", 10002), Handler)
    print("ext_authz listening on :10002", flush=True)
    server.serve_forever()
```
</Accordion>

Your ext_authz service must:

- Return HTTP `200` with provider-specific headers on every request.
- Include `x-envoy-auth-headers-to-remove: x-langsmith-llm-auth` to strip the JWT before forwarding.
- Read the API key from an environment variable or secret store.

## Set up request/response transformation (ext_proc)

For use cases that require request or response body transformation (not just header injection), enable the transformer integration. This uses Envoy's [ext_proc filter](https://www.envoyproxy.io/docs/envoy/latest/configuration/http/http_filters/ext_proc_filter).

Your transformer service must implement the `envoy.service.ext_proc.v3.ExternalProcessor` gRPC service.

```yaml
authProxy:
  transformer:
    enabled: true
    serviceUrl: "grpc://my-transformer:50051"
    timeout: "10s"
    failureModeAllow: false
    processingMode:
      requestHeaderMode: "SEND"
      requestBodyMode: "BUFFERED"
      responseHeaderMode: "SKIP"
      responseBodyMode: "NONE"
```

Set `failureModeAllow` to `true` to fail open (allow requests through) on transformer errors. The default (`false`) rejects requests when the transformer is unavailable.

### When to use ext_authz vs ext_proc

| Capability | ext_authz | ext_proc (transformer) |
|-----------|-----------|------------------------|
| Modify request headers | Yes | Yes |
| Modify response headers | No | Yes |
| Modify request body | No | Yes |
| Modify response body | No | Yes |
| Protocol | HTTP | gRPC |

Use **ext_authz** if you only need to inject auth headers (such as API keys). Use **ext_proc** if you need to rewrite request or response bodies. Both can be enabled simultaneously.

### Processing modes

Control which phases are sent to the transformer:

| Mode | Options | Description |
|------|---------|-------------|
| `requestHeaderMode` | `SEND`, `SKIP`, `DEFAULT` | Whether to forward request headers. |
| `responseHeaderMode` | `SEND`, `SKIP`, `DEFAULT` | Whether to forward response headers. |
| `requestBodyMode` | `NONE`, `BUFFERED`, `STREAMED`, `BUFFERED_PARTIAL` | How to send the request body. |
| `responseBodyMode` | `NONE`, `BUFFERED`, `STREAMED`, `BUFFERED_PARTIAL` | How to send the response body. |
| `requestTrailerMode` | `SEND`, `SKIP` | Whether to forward request trailers. |
| `responseTrailerMode` | `SEND`, `SKIP` | Whether to forward response trailers. |

`BUFFERED` buffers the entire body before sending, which is simplest for transformations. `STREAMED` sends chunks incrementally (lower latency but more complex to implement). Use `NONE` to skip body processing.

<Warning>
When mutating the body, your ext_proc service must also set the `content-length` header to match the new body size via `HeaderMutation` in the body response. Envoy rejects responses where `content-length` does not match the mutated body.
</Warning>

## Configure HTTP proxy

Envoy does not respect `HTTP_PROXY`/`HTTPS_PROXY`/`NO_PROXY` environment variables. To route upstream traffic through an HTTP proxy, configure it explicitly:

```yaml
authProxy:
  httpProxy:
    enabled: true
    host: "proxy.example.com"
    port: 3128
    noProxy:
      - "internal.corp"
      - ".internal.corp"
```

## Configure ingress

The auth proxy uses a separate ingress from the main LangSmith application, optimized for streaming. Configure it with your preferred ingress method:

```yaml
ingress:
  enabled: true
  ingressClassName: "nginx"
  annotations:
    nginx.ingress.kubernetes.io/proxy-buffering: "off"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
  hosts:
    - host: llm-proxy.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: llm-proxy-tls
      hosts:
        - llm-proxy.example.com
```

Alternatively, use the Gateway API:

```yaml
gateway:
  enabled: true
  name: "my-gateway"
  namespace: "gateway-system"
  hostnames:
    - "llm-proxy.example.com"
```

## Configure autoscaling

Enable horizontal pod autoscaling for production deployments:

```yaml
authProxy:
  autoscaling:
    hpa:
      enabled: true
      minReplicas: 2
      maxReplicas: 10
      targetCPUUtilizationPercentage: 50
```

Default resource requests are `100m` CPU / `128Mi` memory, with limits of `500m` CPU / `256Mi` memory. Adjust these based on your traffic volume:

```yaml
authProxy:
  deployment:
    resources:
      requests:
        cpu: "200m"
        memory: "256Mi"
      limits:
        cpu: "1000m"
        memory: "512Mi"
```

## Full configuration example

The following example configures the proxy with JWT validation, ext_authz for API key injection, autoscaling, and ingress:

```yaml
authProxy:
  upstream: "https://gateway.example.com"
  jwtIssuer: "langsmith"
  jwtAudiences:
    - "my-llm-gateway"
  jwksUri: "https://langsmith.example.com/.well-known/jwks.json"
  jwksCacheDurationSeconds: 300
  streamIdleTimeout: "300s"

  extAuthz:
    enabled: true
    serviceUrl: "http://my-auth-service:8080"
    timeout: "10s"

  autoscaling:
    hpa:
      enabled: true
      minReplicas: 2
      maxReplicas: 10

ingress:
  enabled: true
  ingressClassName: "nginx"
  hosts:
    - host: llm-proxy.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: llm-proxy-tls
      hosts:
        - llm-proxy.example.com
```

## Troubleshoot

### JWT validation fails (401 on valid JWT)

- Verify the JWKS matches your signing key. If using `jwksUri`, confirm the endpoint is reachable from the proxy pod.
- Confirm `jwtIssuer` and `jwtAudiences` match the claims in your JWT.
- Check the JWKS cache: if you recently rotated keys, wait for `jwksCacheDurationSeconds` to expire or restart the proxy.

### 403 on valid JWT

- The JWT signature is valid but the `aud` claim does not match any value in `jwtAudiences`.

### ext_authz errors (503)

- Verify the ext_authz service is running and reachable at the configured `serviceUrl`.
- Check ext_authz container logs for errors.
- Confirm the service returns `200` with the expected headers.

### Streaming responses time out

- Increase `streamIdleTimeout` (default `300s`). This controls how long Envoy waits between chunks in a streaming response.

## Helm chart reference

For the full list of configurable values, see the [Helm chart README](https://github.com/langchain-ai/helm/tree/main/charts/langsmith-auth-proxy).
