---
title: Managed Deep Agents
sidebarTitle: Overview
description: Build your agent as a directory of files while LangSmith runs the harness and runtime.
---

Managed Deep Agents (MDA) is the simplest way to build and deploy production agents. You focus on what your agent does. MDA runs it. There are no servers to run and no infrastructure to wire together.

You write the agent's intelligence: its instructions, the tools it can call, the skills it follows, and you select the model that drives it. MDA provides everything underneath:

- **The Deep Agents harness**: The agent loop that plans, calls tools, manages a filesystem, and delegates to subagents. See [Deep Agents](/oss/deepagents/overview).
- **A managed runtime**: [LangSmith Deployment's Agent Server](/langsmith/agent-server-overview) hosts and operates the agent, and keeps sessions running across restarts.

```mermaid actions={false}
%%{init: {"theme": "base", "themeVariables": {"fontFamily": "Inter, sans-serif", "lineColor": "#40668D", "primaryColor": "#E5F4FF", "primaryTextColor": "#030710", "primaryBorderColor": "#006DDD"}}}%%
flowchart LR
    subgraph you["You provide"]
        direction TB
        LogicHdr["<b>Business logic</b>"]
        LI1(["Instructions"])
        LI2(["Tools"])
        LI3(["Skills"])
        LI4(["Model"])
        LogicHdr -.- LI1
        LogicHdr -.- LI2
        LogicHdr -.- LI3
        LogicHdr -.- LI4
    end

    subgraph mda["Managed Deep Agents"]
        direction TB
        HarnessHdr["<b>Deep Agents harness</b>"]
        HI1(["Agent loop"])
        HI2(["Filesystem"])
        HI3(["Subagents"])
        HarnessHdr -.- HI1
        HarnessHdr -.- HI2
        HarnessHdr -.- HI3

        RuntimeHdr["<b>Managed runtime</b>"]
        RI1(["Agent Server"])
        RI2(["Sandboxes"])
        RI3(["Schedules"])
        RuntimeHdr -.- RI1
        RuntimeHdr -.- RI2
        RuntimeHdr -.- RI3

        HarnessHdr ==> RuntimeHdr
    end

    LogicHdr ==> HarnessHdr

    classDef inputHdr fill:#F6FFDB,stroke:#6E8900,stroke-width:2px,color:#2E3900,font-weight:600
    classDef inputItem fill:#FBFFEC,stroke:#A9C34A,stroke-width:1px,color:#2E3900

    classDef harnessHdr fill:#E5F4FF,stroke:#006DDD,stroke-width:2px,color:#030710,font-weight:600
    classDef harnessItem fill:#F4FAFF,stroke:#7FC8FF,stroke-width:1px,color:#030710

    classDef runtimeHdr fill:#EBD0F0,stroke:#885270,stroke-width:2px,color:#441E33,font-weight:600
    classDef runtimeItem fill:#F7E9FA,stroke:#C79BD0,stroke-width:1px,color:#441E33

    class LogicHdr inputHdr
    class LI1,LI2,LI3,LI4 inputItem
    class HarnessHdr harnessHdr
    class HI1,HI2,HI3 harnessItem
    class RuntimeHdr runtimeHdr
    class RI1,RI2,RI3 runtimeItem

    style you fill:none,stroke:#40668D,stroke-width:1px,stroke-dasharray:4 3
    style mda fill:none,stroke:#40668D,stroke-width:1px,stroke-dasharray:4 3
```

## Example agent

TRUNCATED PLACEHOLDER